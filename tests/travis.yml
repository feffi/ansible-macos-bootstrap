---
- hosts: 127.0.0.1
  connection: local
  vars:
    DEBUG: true
    macos_include_vars: {
      paths: [
        "./.macos-bootstrap"
      ]
    }
  roles:
    - { role: ansible-macos-include-vars }